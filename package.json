{
  "name": "openscreen",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "Bare-bones CLI tools for video post-processing — zoom/pan, backgrounds, annotations, crop, trim. Generates FFmpeg filter strings and commands for use in video pipelines.",
  "scripts": {
    "build": "cd cli && npm run build",
    "dev": "cd cli && npm run dev",
    "start": "node cli/dist/index.js"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.2.2"
  }
}
